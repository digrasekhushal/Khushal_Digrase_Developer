 // number datatype ----------
      let num = 20;
      console.log(num);
      // string datatype ----------
      let str = "Hey, I am leaning javaScript!";
      console.log(str);

      // boolean datatype ----------
      let bool = true;
      console.log(bool);

      // character datatype ----------
      let char = "A";
      console.log(char);

      // float datatype ----------
      let float = 20.5;
      console.log(float);

      // array datatype ----------
      let arr = [1, 2, 3, 4, 5];
      console.log(arr);

      // Dictionary datatype ----------
      let dict = { name: "Khushal", age: 19 };
      console.log(dict);

      // object datatype ----------
      let obj = {
        name: "Khushal D",
        age: 19,
        city: "Katol",
      };
      console.log(obj);

      // null datatype ----------
      let nullVar = null;
      console.log(nullVar);